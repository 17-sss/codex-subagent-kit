# Tasks: TypeScript Package CI

## Phase 1: Workflow

- [x] Extend the PR CI workflow with a TypeScript package validation job.
- [x] Keep the Python gate in the same workflow.

## Phase 2: Validation

- [x] Run npm install, test, typecheck, build, and dry-run packaging inside CI.
- [x] Update the testing and release docs to reflect the expanded PR gate.

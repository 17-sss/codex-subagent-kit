# Tasks: Release Label Sync

- [x] Add `.github/labels.yml` as the source of truth for release labels.
- [x] Add a GitHub Actions workflow that syncs labels from that YAML file.
- [x] Document the labels-sync workflow in release documentation.

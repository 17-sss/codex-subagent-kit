# Tasks: TypeScript TUI Port

## Phase 1: Interactive Flow

- [x] Port the install-first interactive flow to TypeScript.
- [x] Keep the project-scope root orchestrator guardrail and default selection behavior.

## Phase 2: CLI Wiring

- [x] Wire the bare command entrypoint to the TypeScript TUI implementation.
- [x] Wire the explicit `tui` command to the same implementation.

## Phase 3: Validation

- [x] Add TypeScript tests for default selection and project/global validation behavior.
- [x] Add TypeScript tests for clean validation exit codes, issue exit codes, and prompt exit handling.

# Tasks: TypeScript Workspace Bootstrap

## Phase 1: Workspace Skeleton

- [x] Add a root npm workspace definition for TypeScript packages.
- [x] Add a dedicated `packages/codex-subagent-kit/` package with TypeScript build config.

## Phase 2: CLI Bootstrap

- [x] Add a `codex-subagent-kit` CLI entrypoint for the TypeScript workspace.
- [x] Limit the initial command skeleton to the stable TypeScript port scope.
- [x] Make the bootstrap explicitly point back to the Python CLI until parity exists.

## Phase 3: Documentation

- [x] Update the TypeScript port planning docs with the concrete workspace path.
- [x] Reflect the workspace bootstrap in project-facing docs.
